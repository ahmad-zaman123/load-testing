# Copy to staging.env (gitignored) and fill in.
# Then: `source config/staging.env && make smoke`

# Base URL of the target API
HOST=https://staging-api.your-domain

# Path to the token fixture (generated by the backend's seed_load_test_users command,
# then scp'd here).
LOAD_TEST_TOKEN_FIXTURE=fixtures/tokens.json
